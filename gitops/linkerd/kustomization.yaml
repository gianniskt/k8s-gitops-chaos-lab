apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - linkerd-repo.yaml
  # linkerd-crds.yaml removed - CRDs are installed by Traefik (Gateway API) and linkerd-control-plane (Linkerd-specific)
  - linkerd-control-plane.yaml
  - linkerd-viz.yaml
  - linkerd-ingress.yaml